# Relative path recommended (keeps results inside the project)
results_dir: storage/results/

llm_generation_defaults:
  temperature: 0.2
  num_predict: -1

image_generation_defaults:
  width: 512
  height: 512
  seed: -1